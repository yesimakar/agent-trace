# AgentTrace Web

React + TypeScript dashboard for reviewing AI-agent runs, timelines, metrics, and errors.

## Run Locally

Start the API first, then run:

```bash
cp .env.example .env
npm install
npm run dev
```

Open:

```text
http://127.0.0.1:5173
```
